angular.module('hg')
.controller('HeaderCtrl', ['$scope', '$route',
  function($scope, $route){
    
    /*
    var app = null;
    var thisUrl = utilService.here;
    if(thisUrl.toLowerCase().indexOf('jobdescription') !== -1){ app = 'Job Description'; } 
    else if(thisUrl.toLowerCase().indexOf('configuration') !== -1 || thisUrl.toLowerCase().indexOf('admin') !== -1){ app = 'Configuration'; }
        
    if($route.current && $route.current.templateUrl){
      var url = $route.current.templateUrl.toLowerCase();
      if(app === 'Job Description'){
        $scope.pageName = 'JobDescription';
      }
      else if(app === 'Configuration'){
        $scope.pageName = 'Configuration';
      }
      else{ $scope.pageName = ''; }
    }
    else{ $scope.pageName = ''; }
    
    $scope.home = function(){
      window.top.location.href = '/#/home';
    };
    
    $scope.notifications = function(){
      window.top.location.href = '/#/notifications';
    };
    
    $scope.settings = function(){
      window.top.location.href = '/#/settings';
    };
    
    $scope.logout = function(){
      window.top.location.href = '/#/logout';
    };
    */
    
  }
]);
